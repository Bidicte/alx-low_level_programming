#include <stdio.h>
#include <string.h>

/**
 * _strlen - the function that shows the length of sentence or word
 *@s: the string
 *
 * Return: 0
 */

int _strlen(char *s)
{
int i;
for (i = 0; s[i] != '\0'; ++i)
{
printf("%d\n",strlen(i));
}
return (0);
}
